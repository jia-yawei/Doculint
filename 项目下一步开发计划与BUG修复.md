# 项目下一步开发计划与 BUG 修复

## 当前待验证事项

以下代码优化已完成，但需要在真实 Word 文档中回归确认后才能关闭：

### BUG-01 标识提取完整性回归

- 已支持项目编号中包含 `/` 的标识，例如 `Q2460-2089/2099-CSCI-SRS-001`。
- 仍需用真实样本文档验证：长标识、三位及四位编号、不同文档类型下均不截断。
- 验收：提取结果与原文标识完全一致。

### BUG-03 首次插入总页码回归

- 插入前后已增加重新分页、页数统计和域更新。
- 仍需验证首次插入、删除后重插、页眉页脚及多节文档场景。
- 验收：首次插入即与 Word 实际总页数一致。

### OPT-01 “搞快点”页签响应回归

- 已延迟初始化 Ribbon，并跳过选区位置未变化时的重复样式状态刷新。
- 仍需在打开大文档后首次切换页签，以及多文档切换时确认无明显卡顿。
- 验收：首次切换和常规选区移动时无明显停顿。

## 后续开发

### 需求追踪模块回归

- 用两份已执行“需求提取并保存”的文档验证互追导入。
- 确认左右两侧完整展示各自已保存的需求，不因模板前缀过滤遗漏。
- 验证默认 `SRS -> SDS` 和“自定义”表头导出结果。
- 验证“提取 -> 保存 -> 导入互追 -> 映射 -> 导出追踪表”完整链路。

## 已关闭

- 追踪表中的需求标识不再写成 `...`，改为完整写入。
- “导入互追文档”已改为标准文件选择对话框，可选择已打开或磁盘中的 Word 文档。
