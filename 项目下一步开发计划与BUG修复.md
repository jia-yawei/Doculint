# 项目下一步开发计划与BUG修复

## 1. 当前重点

下一阶段先不铺新功能，优先收敛需求提取与追踪表链路，先把“提取得准、插得全”做稳定。

当前优先级：

1. 修复需求提取中的标识提取问题
2. 修复需求插入追踪表时标识被省略为 `...` 的问题
3. 修复插入总页码与实际页码不一致的问题
4. 修复需求追踪导入互追文档时无法弹出选文档对话框的问题
5. 优化打开 Word 后切换到“搞快点”插件页签时的卡顿问题
6. 完成需求追踪模块可用性回归验证
7. 在问题收敛后再继续补充需求追踪模块剩余功能

## 2. 已知BUG

### BUG-01 标识提取不规范，且存在残缺

现象：

- 需求提取过程中，需求标识提取结果不完整
- 某些标识存在截断、缺字、格式不统一的问题
- 提取结果与原文中的完整需求标识不一致

影响：

- 后续需求映射和追踪关系建立不可靠
- 同一条需求可能被错误识别为不同需求
- 追踪表插入、导出、比对都会受到影响

修复目标：

- 保证需求标识按原文完整提取
- 保证提取结果格式统一，不丢字符、不截断
- 对常见标识格式做覆盖验证

建议处理步骤：

1. 排查需求提取模块中“标识识别”和“标识清洗”逻辑
2. 检查正则表达式、分段裁剪、字符串截断逻辑
3. 用真实文档样本回放，确认是提取阶段出错还是显示阶段出错
4. 修复后补一个最小回归样例，确保完整标识不会再被截断

### BUG-02 插入追踪表时需求标识显示为 `...`

现象：

- 已提取出的需求在插入追踪表时，需求标识没有完整写入单元格
- 结果显示为简化后的 `...`

影响：

- 追踪表失去可读性
- 用户无法直接通过追踪表确认完整需求标识
- 后续审查和导出会出现误判

修复目标：

- 插入追踪表时写入完整需求标识
- 追踪表中不再出现用 `...` 代替完整标识的情况
- 如果是单元格展示问题，要保证数据本身仍然完整可取

建议处理步骤：

1. 排查追踪表插入逻辑中的文本写入过程
2. 检查是否存在显示用截断、预览用缩略、长度限制处理
3. 区分“数据被截断”还是“界面显示省略”
4. 修复后用完整长标识做插表验证

### BUG-03 插入总页码后结果不正确

现象：

- 插入总页码后，显示结果与文档真实总页码对不上
- 删除已有总页码后重新插入，结果又恢复正确

影响：

- 页码域结果不可信
- 用户需要重复删除再插入，操作成本高
- 文档交付前容易带出错误页码

修复目标：

- 首次插入总页码时即得到正确结果
- 避免依赖“先删除再插入”才能修正
- 明确是域更新问题、书签范围问题还是旧字段复用问题

建议处理步骤：

1. 排查插入总页码逻辑是否复用了旧域或旧范围
2. 检查插入后是否立即执行了域更新
3. 检查总页码引用的对象范围是否正确
4. 用“首次插入”和“删除后重插”两种路径做对比验证

### BUG-04 需求追踪导入互追文档时无法弹出选择文档对话框

现象：

- 在需求追踪模块中执行“导入互追文档”时，未正常弹出选择文档对话框
- 用户无法继续选择目标文档

影响：

- 互追文档导入流程中断
- 需求追踪模块关键入口不可用

修复目标：

- 触发导入互追文档时可以正常弹出文档选择对话框
- 选择文档后能继续完成后续导入流程

建议处理步骤：

1. 排查按钮事件是否实际触发
2. 检查文件选择对话框创建与显示逻辑
3. 检查当前线程、窗口宿主或权限条件是否拦截了弹窗
4. 用空文档、已有文档、多窗口场景分别验证

### OPT-01 打开 Word 后切换到“搞快点”插件页签时卡顿

现象：

- 打开 Word 后，首次切换到“搞快点”插件页签时响应较慢
- 用户感知为界面卡顿、切换不流畅

影响：

- 首次使用体验差
- 用户会误判为插件不稳定或无响应

优化目标：

- 降低首次切换到插件页签时的卡顿
- 避免在页签切换瞬间执行过重初始化逻辑
- 将必要初始化前移或延后到真正使用时再执行

建议处理步骤：

1. 排查 Ribbon 加载、状态刷新、文档扫描、窗格初始化是否集中发生在页签切换时
2. 识别可以延迟加载的逻辑，避免首次切换时一次性执行
3. 检查是否存在重复扫描文档、重复读取样式或重复刷新按钮状态
4. 用首次打开文档和多文档切换场景做对比验证

## 3. 下一步开发计划

### 第一阶段：修复需求提取链路

目标：

- 修正需求标识提取不完整的问题
- 保证提取结果可直接用于后续追踪表插入

输出：

- 标识提取逻辑修复
- 典型样本文档回归通过

### 第二阶段：修复追踪表插入链路

目标：

- 保证追踪表中插入的是完整需求标识
- 保证插入结果与提取结果一致

输出：

- 追踪表插入逻辑修复
- 长标识、多条需求、边界长度样例验证通过

### 第三阶段：修复页码与导入入口问题

目标：

- 修复总页码首次插入不正确的问题
- 修复互追文档导入入口不可用的问题

输出：

- 总页码插入逻辑修复
- 导入互追文档入口恢复可用

### 第四阶段：优化插件页签切换性能

目标：

- 优化首次切换到“搞快点”页签时的响应速度
- 减少不必要的初始化和重复计算

输出：

- 首次切换卡顿问题得到缓解
- 初始化时机更合理

### 第五阶段：回归需求追踪模块

目标：

- 对“提取 -> 选择 -> 插表/映射”链路做一次完整回归
- 确认需求追踪模块具备重新开放的基础

输出：

- 已知问题关闭清单
- 可重新开放功能的判断结论

## 4. 验证建议

建议至少准备以下测试样本：

1. 标识较短的标准需求
2. 标识较长、层级较深的需求
3. 带特殊分隔符的需求标识
4. 连续多条需求提取并插入追踪表的场景
5. 首次插入总页码与删除后重插的对比场景
6. 导入互追文档并弹出文件选择对话框的场景
7. 打开 Word 后首次切换到“搞快点”页签的性能场景

验收标准：

- 提取出的需求标识与原文一致
- 插入追踪表后的需求标识与提取结果一致
- 全链路中不出现残缺、截断、`...` 代替完整标识的问题
- 插入总页码后与文档真实页数一致
- 导入互追文档时可以正常弹出并完成文档选择
- 首次切换到“搞快点”页签时无明显卡顿
